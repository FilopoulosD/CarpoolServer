<div class="container-fluid window">
    <div class="row">
        <div class="col ">
            <p class="Kalam question">Are you sure you want to exit ride No?</p>
        </div>
    </div>
    <div class="row text-center">   
        <div class="text-center">
            <form action="/end/{{{id}}}" method="post">
                <button class="btn rounded-pill Montserrat button">End ride</button>
            </form>
            
            <a class="btn rounded-pill Montserrat button" href="/ride/{{{id}}}" role="button">Go back</a>
        </div>
        
    </div>